import React from 'react';
import { Box } from '@mui/material';
import './Home.scss';
import PokemonCard from '../../components/PokemonCard';

const Home: React.FC = () => {
  return (
    <Box component="div">
    </Box>
  );
};

export default Home;
